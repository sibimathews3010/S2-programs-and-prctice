#include<stdio.h>
int main()
{
    int a,b;
    //printf("enter num1:");
    scanf("%d",&a);
   // printf("enter the num2:");
    scanf("%d",&b);
    if(a>b)

       { printf("%d is the largest",a);}

    else
    {
        printf("%d is the largest" ,b);}
 return 0;
}
